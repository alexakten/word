import { Cardo, Inter } from "next/font/google";

export const cardo = Cardo({
  subsets: ["latin"],
  weight: "400",
  display: "swap",
});

export const inter = Inter({
  subsets: ["latin"],
  display: "swap",
});
